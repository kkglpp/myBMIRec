import 'package:flutter/material.dart';

class SelectSlider extends StatelessWidget {
  const SelectSlider({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}